Provide an instance id, snapshot description, and device names to attach a series of Amazon AWS EBS snapshots to an EC2 instance.

* See settings.json for options
* Run with `node index.js --config=settings.json`
